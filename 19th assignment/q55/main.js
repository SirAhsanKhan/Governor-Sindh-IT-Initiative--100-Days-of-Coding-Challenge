//initialize an array
var numbers = [5, 6, 7, 2, 9];
// multiplying each number by 2
var multipliedNumber = numbers.map(function (number) { return number * 2; }); //here .map is a method that performs a function and transfer results to new array 
console.log(multipliedNumber);
